<%= form_for(@agenda) do |f| %>
  <% if @agenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda.errors.count, "error") %> prohibited this agenda from being saved:</h2>

      <ul>
      <% @agenda.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :agenda_id %>
    <%= f.text_field :agenda_id %>
  </div>
  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :event %>
    <%= f.text_area :event, :class => 'ckeditor', :input_html => { :height => 400 } %>
  </div>
  <div class="field">
    <%= f.label :venue %>
    <%= f.text_area :venue, :class => 'ckeditor', :input_html => { :height => 400 } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
